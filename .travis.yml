language: python

python:
   - "3.5"

sudo: false

notifications:
  slack: cs373-spring-2016:UHDS6Yf8gT72UqMcsSnqwXmu

install:
    - pip install --upgrade pip
    - pip --version
    - pip install coverage
    - pip install numpy
    - pip install pylint

before_script:
    - uname -a
    - printenv
    - pip list
    - coverage --version
    - pylint   --version

script:
    - date
    - ls -al
    - make test
